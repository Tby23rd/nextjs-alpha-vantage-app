// pages/index.tsx

import Stocks from './Stocks'; // Assuming Stocks.tsx is in the same directory

const HomePage: React.FC = () => {
  return (
    <div>
      <Stocks />
    </div>
  );
};

export default HomePage;
